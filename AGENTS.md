# Agent Working Guide & Development for PicoClaw

## Codebase Map
See `CODEBASE-MAP.md` for the full codebase context map (generated by mapping-codebase skill).

## Core Responsibilities
- Maintain the ultra-lightweight nature of the project (<10MB RAM).
- Ensure cross-platform compatibility (x86, ARM, RISC-V).
- Optimize for performance on low-resource hardware.

## Build & Test
- `make build` - Build the project
- `make test` - Run tests
- `make deps` - Install dependencies

## Project Structure
- `cmd/picoclaw/` - CLI entry point
- `pkg/` - Core logic packages
- `workspace/` - Default agent workspace

## Key Files for Agents
- `pkg/agent/loop.go` - Main processing logic.
- `pkg/tools/` - Extension point for new capabilities.
- `pkg/channels/` - Communication interface implementations.

## Memory & State
- Agent memory is stored in `workspace/memory/MEMORY.md`.
- Session history is kept in `workspace/sessions/`.
- System state is managed via `pkg/state/`.

## Style Guide
- Use standard Go formatting (`gofmt`)
- Follow effective Go patterns
- Document public interfaces

## Workflow Discipline
- When implementing a user-requested behavior change, record the rule/expectation in `AGENTS.md` in the same session.
- Keep Telegram command behavior and docs aligned (`/help`, command registration, and README command list).

## Session Rules
- 2026-02-23: Tool execution must support configurable policy via `tools.policy` (`deny_by_default`, `deny_list`, `allow_list`, `notify_on_block`); `deny_list` must block specific tools even when deny-by-default is off, and blocked attempts must emit channel-visible `[SYS]`-prefixed notice when notification is enabled.
- 2026-02-23: Subagent cancel provenance must be explicit in runtime/API (`cancel_by`), dashboard must keep subagent status live via fallback refresh, and LLM-triggered `subagent_cancel` must stay disabled by default unless `tools.spawn.allow_llm_cancel=true`.
- 2026-02-23: LLM streaming must be runtime-configurable via `agents.defaults.llm_stream_mode` (`auto|on|off`) with fallback chain `agents.<name>.llm_stream_mode` > `agents.subagents.llm_stream_mode` > `agents.defaults.llm_stream_mode`; for Z.AI no-tool calls `auto` may enable SSE streaming with safe non-stream fallback parsing, and summarization must use retry-aware timeout budget so retry/backoff window is not cut off by single-call `llm_timeout`.
- 2026-02-23: `picoclaw memopt --llm` must skip short `MEMORY.md` files by default using configurable threshold `--llm-min-chars` (default `8000`), and LLM retries in memopt must not be cut off by a short outer CLI context timeout.
- 2026-02-20: Telegram `/status` must display named subagent identity (`Agent: <name>`) when a subagent was started with `name`.
- 2026-02-20: Prefer `Makefile` targets as the primary way to build/test/run project tasks.
- 2026-02-20: Do not run tests via direct `go test`; always execute tests through `make` targets (e.g., `make test`, optionally with `TEST`/`RUN` variables).
- 2026-02-20: Before any deletion, agent must inspect path and content provenance first; ask user only when uncertain whether it is safe disposable clutter.
- 2026-02-20: Subagent completion must never be "silent"; max-iteration exhaustion is an error and empty final content must use explicit fallback text.
- 2026-02-20: Named-agent memory writes must only target canonical `.../agents/<name>/memory`; block mirrored `.picoclaw/workspace/agents/...` paths inside project directories.
- 2026-02-20: `./picoclaw/nanobotnb` is read-only downloaded state; never edit files there.
- 2026-02-20: Startup preflight skill-lint issues are warning-only; on gateway start inject warning summary into `idle` session for autonomous cleanup.
- 2026-02-20: Priority controls are channel-agnostic via configurable prefix (`ingress.concat_prefix`, first rune): `<prefix>status` returns immediate runtime status, `<prefix>inject` bypasses queue when no active run exists, `<prefix>first` enqueues at queue head, `<prefix>kill` cancels subagent task; `<prefix><prefix>MESSAGE` appends to previous queued message in same session and must return explicit append confirmation.
- 2026-02-20: Validation and release updates must match change scope: runtime/code-path changes require relevant fmt/lint/tests plus docs/changelog/version in the same pass; docs-only or operational-chore edits do not require full build/test cycle or version bump.
- 2026-02-20: User may edit `TODO.md` during implementation; treat those edits as intentional collaborative updates and continue without escalation.
- 2026-02-20: Parallel user edits are expected; do not stop on unrelated concurrent changes. Pause only for direct conflicts in files being edited or before destructive actions.
- 2026-02-20: Completed items that are already recorded in `CHANGELOG.md` and committed must be removed from `TODO.md` (do not keep checked done items there).
- 2026-02-22: Release hygiene is mandatory in the same implementation pass: when a behavior change is completed, update `CHANGELOG.md` and remove corresponding finished items from `TODO.md` in the same commit set (plus `README.md`/`VERSION` when applicable).
- 2026-02-20: If user intent or requirements are ambiguous, confirm implementation direction before making code edits.
- 2026-02-20: Use native edit tools directly for patches (`apply_patch` tool), not shell-wrapped patch flows.
- 2026-02-20: Changelog entries for behavior changes must explicitly enumerate new user-visible fields/metadata.
- 2026-02-20: Run `make vet` early after structural/API edits, not only at final verification pass.
- 2026-02-20: Respect explicitly requested documentation location; do not add parallel doc edits elsewhere unless requested.
- 2026-02-20: For live-runtime incident diagnosis in this repo, first use skill-based history/log workflow and project-local log paths under `picoclaw/nanobotnb/.picoclaw/workspace/logs/*`; do not assume Docker access unless explicitly confirmed.
- 2026-02-20: File access hard-blocks must be configurable via `agents.defaults.deny_path_patterns` and enforced even when `restrict_to_workspace` is disabled.
- 2026-02-20: Channel-agnostic prefix controls must be fully intercepted (never forwarded to agent): `<prefix>help|status|models|channels|inject|first|kill|delete` and `<prefix><prefix>MESSAGE` append.
- 2026-02-20: Gateway shutdown must be fail-safe: first signal attempts graceful stop, repeated signal forces immediate exit, and shutdown path must have bounded timeout.
- 2026-02-20: Incident-mode observability must be explicit: console logs should mark subagent flow with visible ID prefix and queue internals must be available via debug logs.
- 2026-02-20: Runtime diagnostics must be configurable in `config.logging.level` with CLI `--debug` kept as an explicit higher-priority override.
- 2026-02-20: Dashboard UX defaults must be live-first: SSE-driven auto updates, manual refresh controls hidden while SSE is connected (visible only as fallback), newest subagents first, and session history auto-scrolled to latest entries.
- 2026-02-20: Dashboard must expose operator-grade runtime inspection (`/api/v1/runtime`) including version/capability counts, allowed control commands, channel status, and sanitized config view.
- 2026-02-20: Dashboard layout must prioritize operations workflow: left full-height history pane, right stacked panels (agents/sessions/queue/message), resizable split, clickable agent/session history filters, and subagent status visual state cues.
- 2026-02-20: Dashboard resize UX must persist across reloads (left/right split and right-panel row heights saved in browser local storage).
- 2026-02-20: Active-run urgent inject must append urgent content into the running context (no cancel/preempt) and be handled by a follow-up LLM iteration in the same run; debug logs must not dump full `messages_json/tools_json` payloads.
- 2026-02-20: Subagent completion events from `system` channel must always be delivered into the origin session context (inject when active; when no active run, persist to history and trigger immediate same-session LLM run), never dropped via outbound `system` routing.
- 2026-02-20: Full LLM request payloads (`messages_json/tools_json`) must remain available in `audit.jsonl` (`llm_request_full` events) for incident analysis, while console/file logs stay concise.
- 2026-02-20: IDLE triggers must be injected into the same main session as a normal message (XML-wrapped idle payload), so the main chat history/context explicitly tracks idle-mode processing.
- 2026-02-20: Default prompt files in `workspace/` must stay minimal and English-only for startup behavior; keep `AGENTS.md`, `IDENTITY.md`, `SOUL.md`, `USER.md`, `IDLE.md`, and `SUBAGENTS.md` present without deployment-specific custom workflows.
- 2026-02-20: `spawn.directory` must define subagent runtime working directory automatically for relative tool paths/commands (exec and file tools), not only prompt context.
- 2026-02-20: Gateway automatic notices must be prefixed via `gateway.sys_message_prefix` and broadcast to all known external sessions for startup, shutdown, and runtime errors (not just last active session).
- 2026-02-20: Loop limits must stay explicit and non-aliased for main agent and subagents: `max_iterations` (total LLM iterations) and `max_tool_iterations` (tool-call iterations) are separate fields in defaults/subagents/named-agent config.
- 2026-02-20: Gateway HTTP server must keep SSE stable (`/api/v1/events`) by using streaming-safe timeouts (no short global write timeout that terminates chunked event streams).
- 2026-02-20: `config/config.example.json` must mirror the current runtime schema, including `agents.subagents`, flat named-agent keys, and all available provider entries/options.
- 2026-02-20: Subagent iteration limit must be unambiguous: resolve from `agents.subagents.max_iterations` with optional `agents.<name>.max_iterations` override; do not use `agents.defaults.max_iterations_subagent`.
- 2026-02-20: `config/config.example.json` is a strict contract and must be kept in exact sync with runtime behavior on every config/code-path change (no delayed or partial updates).
- 2026-02-20: Maintain `config/config.example.json` with the same discipline as `CHANGELOG.md` and `README.md`; any behavior/config surface change must update all three in the same pass.
- 2026-02-20: Dashboard controls must mirror channel-agnostic prefix commands: message modes map to `<prefix>inject|first|delete|<prefix><prefix>`, subagent rows expose `KILL` when available, and queue rows keep reorder+delete controls visible.
- 2026-02-20: Dashboard Agents panel must keep an explicit one-click `Clear filter` control for subagent-history filtering.
- 2026-02-20: Dashboard operator UX must guard accidental actions (confirm destructive controls, busy-state buttons), keep history auto-scroll non-intrusive, and show explicit empty state when subagent filter has no matches.
- 2026-02-20: Exec safety path guard must not treat free-text flag values (`--description`, `--body`, `--message`, `--title`, `-m`) as filesystem paths, while still blocking real absolute path arguments outside workspace.
- 2026-02-20: LLM retry/backoff policy must be implemented once in a shared wrapper and reused by main loop, subagent loop, and summarization calls; do not duplicate retry logic at call sites.
- 2026-02-21: `exec` safety must enforce `agents.defaults.deny_path_patterns` for both absolute and relative shell path arguments (including `.beads/*.jsonl` and `.beads/*.db`), even when `restrict_to_workspace` is disabled.
- 2026-02-21: Scheduled command execution via `cron` must use the same `exec` safety settings (`restrict_to_workspace` and `deny_path_patterns`) as interactive runtime `exec`.
- 2026-02-21: Dashboard web UI filters must be operator-consistent: top-bar `Clear all filters` resets session+agent filters to all-history scope, agent filter clears session selection, history is single-line expandable rows, and mobile layout must avoid long stacked-panel scroll by showing one right-side panel at a time.
- 2026-02-21: Dashboard all-history view must include recent `logging.dir/agent.log` tail lines in addition to per-session message history, runtime inspector text must render true newlines, and message composer textarea must remain viewport-bounded on mobile.
- 2026-02-21: Dashboard default unfiltered history must show maximum available data with bounded limits (large sessions fetch + large `agent.log` tail + UI render cap), rather than narrowing to a single source by default.
- 2026-02-21: Dashboard selection state must be explicit: active filter scope (`ALL`/`SESSION`/`AGENT`) must be visibly labeled and selected rows in Sessions/Agents tables must have strong visual highlight.
- 2026-02-21: Subagent completion context must always reach main-agent session in every case: persist completion as `system` history and queue urgent session context even when no active main run exists.
- 2026-02-21: LLM transient error retry matching must be case-insensitive and explicitly include timeout forms (`context deadline exceeded`, `Client.Timeout exceeded`) so provider/network stalls do not fail subagents on first attempt.
- 2026-02-21: Named/anonymous subagent runtime config must resolve with fallback chain `named > subagents > defaults` for shared loop knobs (`model`, `max_tokens`, `max_iterations`, `max_tool_iterations`, `temperature`, `history_message_threshold`) so sparse subagent config never silently drops to zero-values.
- 2026-02-21: Session summarization retention must be configurable via `agents.defaults.summary_keep_last_messages` (not hardcoded), and runtime logs must expose estimated context usage per LLM request for operator tuning.
- 2026-02-21: Outbound delivery failures (all channels) must be fed back into the same session as urgent system context including channel/chat/error and failed content preview, and Telegram outbound must auto-split oversized responses before send attempts.
- 2026-02-21: Subagent runtime must honor `agents.subagents.memory_threshold` and `agents.subagents.summary_keep_last_messages` (with `agents.<name>` overrides via `named > subagents > defaults`), and LLM debug logging must stay compact (single `LLM iteration` line with absolute context state, no separate `LLM request` spam).
- 2026-02-21: System prompt debug logging must include per-section size breakdown (identity/tools, bootstrap files, skills, named agents, memory long-term/recent, total) to make prompt bloat diagnosable from console/file logs.
- 2026-02-21: For urgent live-incident reports, diagnostics must run fast-triage first from `logs/agent.log` (and `logs/debug.log` as needed), provide immediate preliminary diagnosis, and avoid broader log/memory exploration unless explicitly requested or strictly necessary.
- 2026-02-21: Active-run urgent inject must not preempt/cancel the current run; it must append urgent content into the active run context and trigger a follow-up LLM iteration in the same run so no extra user message is required.
- 2026-02-21: Inject payload semantics must stay neutral: wrap injected text in `<inject_message ...>` without auto-appending urgency/priority directives; operator intent is defined only by injected content.
- 2026-02-21: Subagent toolloop context sizing must honor `agents.defaults.context_window` as the primary runtime window source (not `max_tokens`-derived estimation), with `max_tokens` fallback only when default context window is unset.
- 2026-02-21: Workspace-wide memory optimization must run in explicit safe scope only (`workspace/memory/**` and `workspace/agents/*/memory/**`), with default dry-run and optional apply/LLM passes; never rewrite bootstrap/system prompt files as part of compaction.
- 2026-02-21: Memory optimization must be available as a direct runtime CLI command (`picoclaw memopt`) so operators on binary-only hosts can run it without Makefile access.
- 2026-02-21: Subagent completion immediate trigger payload must include explicit identity metadata in `<inject_message ...>` (`sender_id`, `agent_id`, `label`) so operator history can attribute who finished.
- 2026-02-21: Subagent provider selection must support fallback chain `agents.<name>.provider` > `agents.subagents.provider` > `agents.defaults.provider`, and both `spawn` and `subagent` executions must resolve provider per task (not only global main-agent provider).
- 2026-02-22: Main-agent LLM requests must perform conservative pre-call context trimming (safety estimate + buffer) so provider-side tokenization drift cannot exceed endpoint prompt-token limits.
- 2026-02-22: Dashboard subagent `KILL` control must use direct Admin API cancel endpoint (`DELETE /api/v1/subagents/{id}`), not queued `+kill` message parsing.
- 2026-02-22: Session message persistence must include per-message `timestamp_ms` for stable timeline ordering across dashboard/history views.
- 2026-02-22: Dashboard live history updates must use keyed incremental row patching (no full-list `innerHTML` redraw on each SSE refresh tick).
- 2026-02-22: Outbound policy must be strict: never auto-forward tool `ForUser` payloads (especially `exec`) to external channels; only explicit `message` tool sends and final main-agent response are user-visible, while automatic panic/runtime error notices must always carry `gateway.sys_message_prefix`.
- 2026-02-22: Subagent status/history fallback label text must be `no-label` (not `(unnamed)`) whenever a task label is empty.
- 2026-02-22: Automatic outbound prefixes are split by semantics: gateway/system notices must use `gateway.sys_message_prefix` (`[SYS]` default), while main-loop final outbound replies must use `gateway.auto_final_prefix` (`[AUTO]` default); the two channels must never be conflated.
- 2026-02-22: Runtime/operator logs and session persistence must use JSONL-oriented storage (`agent.jsonl`, `debug.jsonl`, `audit.jsonl`, sessions as `*.jsonl`) to keep dashboard/API ingestion simple and uniform.
